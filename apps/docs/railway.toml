# Railway docs site configuration.
[build]
watchPatterns = ["/apps/docs/**"]
buildCommand = "bun install && bun run build"

[deploy]
startCommand = "bun run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
