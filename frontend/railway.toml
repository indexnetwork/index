# Railway frontend service configuration.
[build]
watchPatterns = ["/frontend/**", "/bun.lock", "/package.json"]
buildCommand = "bun run build:frontend"

[deploy]
startCommand = "cd frontend && bun run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
