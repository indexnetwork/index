export const metadata = {
  title: 'Discovery Protocol MVP',
  description: 'MVP version of the Discovery Protocol',
};

export default function MVPLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 