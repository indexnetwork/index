{
  "id": "indexnetwork-openclaw-plugin",
  "name": "Index Network",
  "description": "Index Network — find the right people and let them find you. Registers the Index Network MCP server on first use and hands off to its guidance.",
  "version": "0.9.0",
  "configSchema": {
    "type": "object",
    "properties": {
      "agentId": {
        "type": "string",
        "description": "Your Index Network agent ID. Found on the Agents page."
      },
      "apiKey": {
        "type": "string",
        "description": "API key linked to your Index Network agent. Generated on the Agents page."
      },
      "protocolUrl": {
        "type": "string",
        "format": "uri",
        "default": "https://protocol.index.network",
        "description": "Base URL of the Index Network backend."
      },
      "negotiationMode": {
        "type": "string",
        "enum": ["enabled", "disabled"],
        "default": "enabled",
        "description": "When set to \"disabled\", pending turns are skipped — Index Network falls back to its system negotiator."
      },
      "deliveryChannel": {
        "type": "string",
        "description": "OpenClaw channel id for delivering Index Network notifications (e.g. \"telegram\"). Required to receive opportunity and test-message deliveries."
      },
      "deliveryTarget": {
        "type": "string",
        "description": "Channel-specific recipient identifier (e.g. your Telegram chat ID). Required to receive opportunity and test-message deliveries."
      }
    },
    "required": [],
    "additionalProperties": false
  },
  "skills": ["./skills"]
}
