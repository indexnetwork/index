- [x] Finalize the package contracts before implementation; checked items in this section record completed design decisions, not implemented source.
  - [x] Map v2's responsibilities into the agreed layout: scheduling from `runner/runner.ts` and context assembly/persistence from `src/host.ts` belong in `src/runner/agent.runner.ts`; `wake.ts` and `brief.ts` become `PrincipalAgent`; `negotiate.ts` becomes `NegotiatorAgent`; shared loop/tool/model mechanics belong in `src/agents/shared/`. Keep transport subscriptions and process startup in the host. Preserve v2's storage and execution behavior except for the explicitly agreed changes below.
  - [x] Keep `wake` as the public intent-work verb.
  - [x] Use one `PrincipalAgent` instance per intent, bound to that intent's identity; keep `AgentRunner` scoped to one principal and responsible for scheduling across intents.
  - [x] Supply the freshest runner-assembled context on every wake, with fields named `profile`, `intent`, `conversation`, and `opportunities`; opportunities carry their standing instructions/stalls. Bind identity at initialization, not a frozen domain snapshot.
  - [x] Treat principal input as an event (`principal.input` in v2), not a separate execution mechanism. Persist the message/answer in the conversation before notifying the runner; `wake` is an operation, not an event.
  - [x] Let the runner route events to the appropriate work: principal input can trigger a principal wake, while a counterparty turn normally triggers negotiation work. Not every event wakes `PrincipalAgent`.
  - [x] Use `AgentRunner.handle(event)` as the event-receiving entry point, preserving v2's routing and scheduling rules. Keep scheduling and read → reason → persist orchestration in the runner rather than treating v2's helper-function split as separate agent capabilities.
  - [x] Keep `AgentRunner.handle(event): void` nonblocking: route events and schedule/coalesce work without waiting for reasoning or persistence to finish. Await execution internally and report background failures through the configured error handler; returning does not promise work completion or durable queuing.
  - [x] Preserve v2's per-intent wake coalescing within each runner: one active wake and at most one pending follow-up. Incoming wake requests do not abort the active wake; the follow-up reads fresh context after it settles. Different intents may run concurrently.
  - [x] Use `AgentRunner.stop(): void` for a one-way stop: stop accepting events and scheduling work, prevent pending follow-up wakes, and request cancellation through one runner-scoped `AbortController`. Return immediately; do not restart a stopped instance.
  - [x] Treat cancellation as cooperative, not graceful draining or rollback of persisted effects. The host owns event unsubscription and process/infrastructure teardown; do not add separate close, drain, or restart APIs.
  - [x] Keep events as notifications referencing persisted information: carry the event kind and routing identifiers, not input text or conversation/domain snapshots. Load current authoritative context when work executes, including coalesced follow-up wakes.
  - [x] Handle intent changes simply: creation, content edits, and resumption request a normal coalesced wake when eligible; pause/archive stop scheduling work for that intent; internal metadata updates do not wake it. Content edits add a wake trigger beyond v2, not a semantic-change classifier or a new negotiation invalidation mechanism; `PrincipalAgent` may still produce no actions.
  - [x] Preserve v2's startup behavior: load and adopt existing active intents without waking them merely because they exist, then schedule eligible turn-zero negotiations awaiting the principal through the normal negotiation path. Otherwise wait for events.
  - [x] Use `AgentRunner.reconcile(): Promise<void>` for host-triggered initial synchronization and reconnection recovery, rather than passing `connected` into `handle()`. Await persisted-state reads, registration updates, and recovery scheduling, not the reasoning runs scheduled; reconciliation failures reject the promise. Keep the agreed initial adoption behavior and v2's subsequent refresh/recovery behavior without separate start, initialize, or recover methods.
  - [x] Define `AgentEvent` as a union discriminated by `type`: `principal.input`, `intent.created`, `intent.updated`, `intent.lifecycle`, and `negotiation.turn`. Every event carries `intentId`; `negotiation.turn` also carries `opportunityId`. Carry no context snapshots or lifecycle status; read current persisted state when handling work. Normalized `intent.updated` means content edited, so the host must exclude metadata-only updates when forwarding events. Keep connection/reconnection outside this union and use `reconcile()`.
  - [x] Name host reads `getProfile` for the principal's profile/facts, `getIntent` for one intent record, `listIntents` for the principal's intents, `getConversation` for an intent's H2A conversation entries, `listNegotiations` for negotiation summaries, and `getNegotiation` for one detailed negotiation record. Retain `listIntents` for reconciliation, with active-intent selection owned by the runner.
  - [x] Use one `Intent` record with `id: string`, `statement: string`, and `status: IntentStatus` for both `getIntent(intentId: string): Promise<Intent>` and `listIntents(): Promise<Intent[]>`. Use the individual read for fresh work context and the list for reconciliation, with active-intent selection in the runner; do not introduce separate summary/detail types while their fields are identical.
  - [x] Keep `opportunity` and `negotiation` distinct: an opportunity is a potential connection, and its negotiation is the associated A2A exchange. The runner converts negotiation records into opportunity inputs as v2 does; do not use the names interchangeably.
  - [x] Keep initiator/responder roles fixed for the entire negotiation within one `NegotiatorAgent` implementation. Require `NegotiateContext.role`; only the original responder may `accept`, even if it later proposes. Narrow model-visible actions and enforce the same permissions in the turn handler. This is an approved tightening of v2; document that protocol/API enforcement outside this package belongs to Seref.
  - [x] Use `appendMessages(intentId, messages)` as the shared host operation for notes, questions, question retirements, briefs, decisions, and stalls in the intent's conversation. The runner converts agent outputs into structured messages; do not introduce separate `saveBrief`, `saveDecision`, or `saveStall` operations. Negotiation turns remain separate protocol writes.
  - [x] Retain `submitTurn(opportunityId, { action, message, expectedTurnCount })`: `NegotiatorAgent` produces the turn, and the runner submits it using the turn count from the negotiation record used for reasoning. The host remains authoritative for authorization, protocol validation, and rejection of stale submissions.
  - [x] Use `findCounterparties(intentId, query, limit)` in place of v2's `discover`, and retain `createOpportunities(intentId, picks)`. Preserve v2's single-tool flow: find candidates across queries, merge and deduplicate by person, rank/cap the results, then create opportunities; do not add a separate model-driven shortlist approval step.
  - [x] Supply each runner with host access already scoped to its principal, as with v2's client; do not pass `principalId` into every host operation. The host still validates ownership and current authorization on reads and writes; binding identity does not freeze permissions.
  - [x] Determine execution eligibility from authoritative host records rather than separate `canWake()` or `canNegotiate()` operations. Use intent lifecycle status and the negotiation's associated intent, settlement state, turn ownership, and permitted protocol actions. Skip ineligible work without invoking reasoning or recording a stall; the host enforces authorization on reads and writes, and `submitTurn()` remains the final authority.
  - [x] Keep single-record host reads as record-or-rejection contracts, not nullable results or fabricated empty context. Genuine empty conversations and lists remain valid results. Report background read failures through `onError`; reconciliation read failures reject `reconcile()`. Never turn a read failure into a negotiation stall; distinguish known ineligibility from being unable to read whether work is eligible.
  - [x] Finalize the principal-scoped host boundary as `AgentHost`, using locally declared records matching v2's consumed data shapes rather than importing its HTTP client. Host-side domain I/O and event transport implementations remain outside the package.
    - [x] Use `getProfile(): Promise<Profile>` with v2's profile fields: `id: string`, nullable `name`, `intro`, `location`, and `timezone`, and `profileConfirmed: boolean`. Return current host data; only confirmed profile facts enter principal reasoning as facts.
    - [x] Retain the existing `IntentStatus` values (`ACTIVE`, `PAUSED`, `FULFILLED`, `EXPIRED`, `ARCHIVED`) for the agreed `Intent` reads; only `ACTIVE` is eligible. This preserves the record vocabulary, not new lifecycle transitions.
    - [x] Use `getConversation(intentId: string): Promise<{ conversationId: string; messages: ConversationMessage[] }>` with oldest-first messages. Retain v2's message fields (`id`, `conversationId`, `senderId`, `role`, `parts`, `createdAt`, optional `senderName`, `senderAvatar`, and `metadata`); the runner reconstructs domain entries. The inbox's optional UI agent-status field is not needed by the package.
    - [x] Use `listNegotiations(): Promise<Negotiation[]>` for the principal's open negotiations and `getNegotiation(opportunityId: string): Promise<NegotiationDetail>`. Retain v2's summary fields: `id`, `opportunityId`, `intentId`, nullable `awaitingUserId`, `outcome`, and `settledAt`, numeric `turnCount`, timestamps, and `counterparty` (`intentId`, `userId`, nullable `name`/`avatar`, `statement`). Retain outcomes `agreed`, `declined`, and `closed`.
    - [x] Let `NegotiationDetail` extend that summary with ordered turns (`turnIndex`, `seatUserId`, `action`, `message`, `createdAt`) and `protocol` (`guidance`, `availableActions`, nullable `blockedReason`, `maxTurns`, `messageLimit`). Keep these authoritative protocol fields rather than inventing eligibility flags or copying protocol rules into the agent.
    - [x] Use `findCounterparties(intentId: string, query: string, limit: number): Promise<Counterparty[]>`, retaining `intentId`, `userId`, `name`, `statement`, `networkId`, and numeric `score`. Use `createOpportunities(intentId: string, picks: CounterpartyPick[]): Promise<{ opportunityId: string }[]>`, with each pick containing `intentId` and `networkId`. Retain v2's five-query tool schema, per-person highest-score deduplication, and 30-result creation cap; host results remain authoritative about which opportunities exist.
    - [x] Use `appendMessages(intentId: string, messages: PrincipalMessage[]): Promise<void>` with the existing entry shape: `id`, `createdAt`, `kind`, `matches`, `text`, optional `questionId`, `scope`, and `options`. Preserve kinds `question`, `answer`, `user`, `message`, and `expire`; wire scopes `intent` and `match`; and match references `{ opportunityId, counterparty: { id, name } }` with nullable counterparty names. These persisted field names are not new domain roles.
    - [x] Keep `submitTurn(opportunityId: string, turn: { action: NegotiationAction; message: string; expectedTurnCount: number }): Promise<NegotiationDetail>`. The returned detail is the host's post-submission record; the runner must not increment a local counter and treat it as authoritative.
  - [x] Retain v2's conversation-backed representation and reconstruction rules; do not introduce a separate store, new metadata encoding, or memory fallback on persistence failure.
    - [x] Read text by joining text parts with newlines, and read question/match metadata from `metadata.principalMessage`. Preserve v2's first-match selection and translate wire scope `match` to agent scope `opportunity`. Recognize agent-authored `Brief: `, `Decision: `, and `Stall: ` prefixes, including their trailing spaces; otherwise use the entry kind or the message role's ordinary user/message kind.
    - [x] Write briefs, decisions, and stalls as `kind: "message"` entries with those same prefixes and opportunity match references. Append a stall's suggested question using `\n\nTo ask: `; as in v2, reconstruction keeps the whole suffix in `stall.reason` rather than inventing a separately recovered `suggestedAsk`.
    - [x] Fold entries oldest-first. Latest briefs and stalls replace their respective values; a valid standing decision replaces the decision and clears the previous stall and answered marker. A principal user/answer entry marks its referenced opportunity answered, or all opportunities already accumulated when unscoped; it does not itself clear the stored stall. Decisions remain standing until replaced, not consumed after one turn.
    - [x] Reconstruct open questions by `questionId`: a question opens it, and an answer or expiry closes it. New questions do not implicitly retire old ones; expiry is not an answer. The runner generates outgoing entry UUIDs and ISO timestamps; a new question uses its entry ID as `questionId`, while expiry gets a new entry ID and references the original question. Preserve `Withdrawn: ` plus the original question text, with v2's `a question that no longer matters.` fallback.
    - [x] Keep brief/decision/stall bookkeeping out of the model-facing H2A conversation and carry it on opportunity inputs instead. Preserve v2's action variants (`brief`, `decision`, `ask`, `note`, `expire`), an empty wake action list as valid silence, one-opportunity briefing actions, and negotiation results `{ turn: { action, message } }` or `{ stall: { reason, suggestedAsk? } }`.
    - [x] Preserve the incremental `onBrief` and `onOpened` flow as supplied callbacks, not imports of another agent or the runner. Await instruction publication before scheduling its negotiation; append only remaining non-instruction actions after the wake, never the same brief/decision twice. Keep instruction-publication failures separate from ordinary model-correctable tool errors: retain the failure and reject the wake after its loop, without starting work from the unpersisted instruction or asking the principal to resolve a host failure.
    - [x] Preserve v2's empty-result handling: a no-output briefing leaves the instruction missing and produces no turn, retaining its transient hold/principal-wake behavior without inventing a persisted brief. A negotiator that records neither a turn nor a stall returns the existing no-result stall; negotiator stalls are persisted, while scheduling skips and failed host reads are not. Intersect protocol actions with v2's standing-decision permissions: `continue` permits all four actions, `accept` permits `accept`/`propose`, `decline` permits `decline`, and `stop` schedules no negotiation. Also apply the fixed-role constraint: an initiator never receives `accept`, including under a principal-layer `accept` decision.
  - [x] Use one complete, bounded reasoning/tool run as the execution unit shared by `PrincipalAgent` and `NegotiatorAgent`, not a single model completion. Support direct model/tool loops and Hermes-native runs through this boundary.
  - [x] Keep shared reasoning execution's return type as `Promise<void>`, preserving v2's completion/failure contract. Agents collect domain outputs through their tool handlers and return actions/instructions or a turn/stall themselves; execution does not return a second copy of the domain result. Preserve incremental instruction persistence during the run.
  - [x] Keep `instructions` and `prompt` as separate execution inputs: `instructions` supplies the agent's role/rules, while `prompt` supplies the current task and relevant context. Both agents prepare their own pair from runner-supplied context and pass it to execution; negotiation evaluation and writing receive the same scoped principal and A2A evidence. Execution implementations do not define separate agent reasoning policies.
  - [x] Retain v2's `Tool` shape: `name`, `description`, `parameters` as JSON Schema, and a `run(input)` handler. Use `Tool<Input = unknown>` with `defineTool()` to retain tool-specific authoring types and expose an unknown-input execution boundary through one unchecked assertion, without generic schema validation; domain-specific handler checks still apply. Each agent supplies its permitted tools; direct execution invokes the provided handlers locally, and Hermes invokes the same package-owned behavior through its external bridge rather than duplicating business logic.
  - [x] Retain agent-supplied `maxSteps` limits of 8 for a principal wake, 1 for one-opportunity briefing, and 3 for a negotiation run. One step is one model response followed by its requested tool calls, not an A2A turn or a single tool invocation. End earlier when the model requests no tools; reaching the cap ends execution with the outputs already collected by the agent.
  - [x] Name the execution cancellation input `abortSignal: AbortSignal`, supplied from the runner's existing `AbortController`. On observed cancellation, stop further execution steps and reject the run; already-running I/O may finish and persisted effects are not rolled back. New events coalesce work rather than aborting execution.
  - [x] Preserve v2's execution error handling: unknown tool names, invalid JSON arguments, and ordinary thrown tool errors become tool-result error text so the model can correct its call within the remaining `maxSteps`. Model-request failures and observed cancellation reject execution; cancellation takes precedence over ordinary tool-error feedback. Do not add an executor-level automatic retry loop or extend the step budget.
  - [x] Include execution metadata `principalId`, `intentId`, `opportunityId` for briefing/negotiation, and `operation` (`wake`, `brief`, or `negotiate`). These identify existing work, not separate deployed agents, and let external executors select sessions without parsing prompts. Keep principal briefing history separate from A2A negotiation history even for the same opportunity; native-session management remains outside the package.
  - [x] Preserve v2's sequential tool invocation within each reasoning run: execute tool calls from one model response in the returned order, awaiting each handler before starting the next. Apply the same rule to direct and native execution; independent reasoning runs may still overlap.
  - [x] Explicitly state in shared instructions that the agent represents a person, its principal; include the principal's name only when the profile is confirmed and the name is nonblank. Keep `principalId` in execution metadata rather than substituting it for a human name. Preserve the current intent and today's UTC date so relative dates can become explicit dates. Both agent layers use the same helper with their own role instructions; direct and native executors receive the prepared text without adding their own agent policy.
  - [x] Supply both agents with an `execute` function typed as `Execute = (input: ExecutionInput) => Promise<void>` for the complete reasoning/tool run. Direct OpenRouter execution and Hermes-native execution implement the same contract; do not introduce an executor class.
  - [x] Finalize `ExecutionInput` with flat fields: `instructions: string`, `prompt: string`, `tools: Tool[]`, `maxSteps: number`, `abortSignal: AbortSignal`, `principalId: string`, `intentId: string`, `operation: "wake" | "brief" | "negotiate"`, and `opportunityId` present for `brief`/`negotiate` and absent for `wake`. Provider configuration, host access, and domain result copies are not execution inputs.
    - [x] Supply `AgentRunner` with `host: AgentHost`, `execute: Execute`, `decisions: Pick<TypeSafeClient, "evaluate">`, and v2's optional `now`, `log`, and `onError` callbacks. Construction does not subscribe to events or start reasoning; the host uses `reconcile()`, `handle()`, `wake()` for manual reasoning requests, and `stop()`. Bind each principal-agent instance to intent identity and execution dependencies, not cached profile or conversation snapshots; keep collected outputs local to each invocation.
    - [x] Retain the optional `now: () => Date` clock for agent-side instruction preparation, defaulting to the current date formatted in UTC; it is not an `ExecutionInput` field. As in v2, persisted entry timestamps use publication time, not this prompt clock. Keep credentials, ordered model settings, and request timeout in the configured direct executor; retain v2's current model defaults and 120-second request timeout without adding tuning options.
    - [x] Retain v2's tool-result serialization: strings pass through, other results are JSON-serialized with null for no value. JSON Schema describes model tool arguments; do not claim or add generic runtime schema validation. Preserve existing handler checks, sequential invocation, agent-owned budgets, cancellation, and failure rules for the whole run; native execution uses the same tools and limits rather than redefining agent behavior.
  - [x] Keep the concrete OpenRouter client and direct model/tool-loop implementation inside the package under `src/agents/shared/`, configured with host-supplied credentials and model settings. Agents consume the whole-run execution contract; `AgentRunner` remains provider-agnostic. Hermes supplies its native execution implementation outside the package.
- [x] Apply the agreed terminology and source layout throughout the package.
  - [x] Use `intent` consistently in contracts, prompts, tools, logs, errors, and documentation; introduce no `signal` domain aliases or compatibility exports. Platform `AbortSignal` terminology remains cancellation-only.
  - [x] Treat `PrincipalAgent` and `NegotiatorAgent` as H2A and A2A layers of the same agent, not separate deployed identities; reserve `seat` for protocol participation.
  - [x] Use `src/runner/agent.runner.ts`, `src/agents/principal.agent.ts`, and `src/agents/negotiator.agent.ts`, with `{domain}.{purpose}.ts` naming for additional files.
  - [x] Keep input/output contracts beside their consumers; extract only genuinely shared contracts and utilities into peer files under `src/agents/shared/`, keeping domain concepts distinct from execution mechanics.
  - [x] Avoid speculative state files, directories, lower-level barrels, mode flags, runner subclasses, and storage-driver abstractions.
- [x] Implement `AgentRunner` in `src/runner/agent.runner.ts`.
  - [x] Define the normalized `AgentEvent` union beside the runner and export it through `src/index.ts`; events reference persisted records without context snapshots.
  - [x] Define and export the six `AgentHost` read contracts beside the runner using package-owned records, preserving v2's consumed shapes without importing its HTTP client; require current, authorized reads and propagate failures rather than fabricating empty context.
  - [x] Define and export the remaining `AgentHost` operation contracts (`findCounterparties`, `createOpportunities`, `appendMessages`, and `submitTurn`) and their package-owned records, preserving v2's wire formats and keeping persistence, authorization, and protocol validation host-owned.
  - [x] Construct and export `AgentRunner` with `AgentRunnerOptions`, retaining host/execution dependencies and optional callbacks without startup side effects; implement idempotent `stop()` through one private `AbortController`.
  - [x] Receive the agreed normalized `AgentEvent` union through nonblocking `handle(event: AgentEvent): void`, reporting background failures through the configured error handler; keep transport subscriptions outside the runner and scope each instance to one principal. Read lifecycle status from persisted state rather than an event payload.
  - [x] Expose `wake(intentId): void` for explicit manual H2A reasoning requests through the same private scheduler, without fabricated events, saved input, answered questions, or released holds.
  - [x] Keep active intent runs, coalesced follow-ups, in-flight negotiations, newly observed stalls, and cancellation bookkeeping in private in-memory maps/sets.
    - [x] Track active and pending wake IDs in private sets, without retaining context snapshots or serializing different intents.
    - [x] Track active negotiations by opportunity and intent before any reads; drop duplicate in-flight requests rather than queuing another turn, while allowing different opportunities to overlap.
    - [x] Track held negotiations and opportunities needing principal work in separate maps; an empty briefing shares transient hold/wake scheduling without becoming a persisted stall. Starting a wake consumes that intent's pending principal-work markers, not its holds or conversation records.
  - [x] Mark an intent's wake as active before the first asynchronous wait; coalesce further wake requests into one follow-up with fresh context, without interrupting active reasoning. Start the follow-up only if the runner and intent remain eligible.
    - [x] Implement nonblocking private `scheduleWake(intentId)` around the existing execution path. Mark activity before any host reads, retain at most one pending wake per active intent, and re-read authoritative context for each follow-up; inactive intents skip reasoning.
  - [x] Implement context assembly and read → invoke → persist orchestration once, calling both agent layers through the same execution path. Use the agreed host reads and assemble `profile`, `intent`, `conversation`, and `opportunities` for principal wakes, deriving opportunity inputs from negotiation records.
    - [x] Implement private fresh-intent, wake, and single-negotiation context loaders without caching domain snapshots, invoking reasoning, or writing records; retain publication references alongside agent inputs and keep the authoritative negotiation record for later submission.
    - [x] Reconstruct conversation entries from text parts and first-match metadata, preserving exact bookkeeping prefixes, wire-scope translation, and question/answer/expiry entries; fold standing briefs, decisions, stalls, and answered markers oldest-first with v2's clearing rules.
    - [x] Lazily reuse one `PrincipalAgent` instance per intent, binding identity and dependencies only and supplying fresh context on each invocation.
    - [x] Implement private `runNegotiation(intentId, opportunityId)` as eligible read → missing-instruction briefing/publication → permission-filtered negotiation → turn submission or stall publication. Propagate read, execution, and write failures unchanged; invoke this path through `scheduleNegotiation`.
    - [x] Implement private `runWake(intentId)` with fresh eligible context and the cached intent-bound principal agent. Publish instructions incrementally, call the runner's `scheduleNegotiation` without waiting for completion, and publish only remaining non-instruction actions after successful execution; empty output remains valid silence. Remove the interim scheduling callback parameters rather than retaining a second path.
  - [x] Supply each negotiation run with its fixed initiator/responder role from authoritative opening state: before the first turn the awaiting seat is the initiator; afterward use the first turn's author, never the latest proposer. Intersect protocol actions, standing-decision permissions, and role restrictions before invoking negotiation reasoning; skip work when no action remains.
    - [x] Prepare `NegotiateContext` with principal conversation and all A2A turns from an eligible read, using the original seat role and the intersection of host permissions, standing decision, and role restriction. Evaluate scoped source evidence through Jev before generation; return no reasoning context for missing instructions, `stop`, or an empty intersection. Use this context in scheduled negotiation runs.
  - [x] Route principal input, intent creation, content edits, and resumption events to the normal wake path; route counterparty turn events to the affected negotiation using its standing instruction.
  - [x] Use the existing authoritative reads to skip reasoning for paused, archived, or otherwise inactive intents, without a separate lifecycle cache or read path. Require the host to exclude metadata-only updates from normalized `intent.updated` notifications; do not add a semantic classifier or abort active runs on lifecycle notifications.
  - [x] Request a one-opportunity instruction from `PrincipalAgent` when one is missing, without triggering a whole-intent run; skip an existing `stop` decision even when its brief is missing.
    - [x] Return a private `unbriefed` result for empty briefing output, distinct from an ineligible skip and a persisted negotiator stall; hold the opportunity and request principal work after the intent's in-flight negotiations finish, without publishing a fabricated instruction or stall.
  - [x] Implement private publication helpers that convert conversation-bound agent outputs into structured messages and await `appendMessages(intentId, messages)`, preserving v2's conversation-backed storage and keeping negotiation turn submission separate; both principal-wake and single-negotiation execution paths are wired.
    - [x] Publish action batches in supplied order with UUIDs and publication-time timestamps; preserve brief/decision prefixes, note text, question IDs/options/wire scopes, and question retirement as `expire` with its original text or the agreed fallback. Empty batches do not write.
    - [x] Keep authoritative counterparty IDs and nullable names, plus historical question text, in private publication context built from existing reads; do not infer references from display labels, perform extra reads while publishing, or expose publication maps to reasoning agents.
    - [x] Await writes and propagate failures unchanged without retries or an in-memory persistence fallback; leave incremental-versus-remaining action selection to the orchestration caller.
  - [x] Await instruction persistence through `appendMessages` before starting its negotiation, allowing negotiation work to begin while the principal run continues other work.
    - [x] Await missing-instruction publication in the single-negotiation path before applying the brief/decision to local context or invoking the negotiator; failed publication cannot authorize a turn.
    - [x] Wire wake `onBrief` to await publication before passing each non-`stop` decision to `scheduleNegotiation`. Filter briefs and decisions out of final publication; failed instruction publication rejects the wake without handing off that batch or publishing remaining outputs, and earlier successful effects are not rolled back.
    - [x] Wire wake `onOpened` to hand off the host-returned opportunity IDs immediately, without a decision or a new briefing inside the wake. Keep new opportunities out of that wake's inputs and use the same negotiation scheduler for their separate briefing and execution.
  - [x] Persist genuine stalls, collect newly observed stalls until the relevant in-flight negotiations finish, and route them to principal work without repeatedly asking about known stalls.
    - [x] Implement private stall publication using `Stall: ` and optional `\n\nTo ask: ` text with the authoritative opportunity reference, and await it for explicit and no-result negotiator stalls in `runNegotiation`; only then register the hold and pending principal work.
    - [x] Release each in-flight slot on completion, skip, or failure. Request a coalesced principal wake only when that intent has no negotiations in flight and has newly pending principal work; successful turns clear their opportunity's hold and pending marker. Other intents do not block this wake, and previously covered stalls alone do not trigger another one.
  - [x] Hold stalled negotiations until principal input or an applicable accept/decline decision releases them.
    - [x] Honor holds on both wake-originated and directly scheduled negotiation work. Among scheduling decisions, only `accept` or `decline` releases a hold; `continue` retains it and a wake's `stop` decision does not schedule work. Fresh host reads still determine actual execution permissions.
    - [x] On `principal.input`, clear existing in-memory holds for that intent only, then request a normal coalesced wake. Do not delete persisted stalls or instructions, alter another intent's holds, or automatically retry every released negotiation; principal reasoning decides what to change.
  - [x] Check eligibility using current `getIntent()` and `getNegotiation()` records, including intent association, lifecycle, settlement, turn ownership, and permitted protocol actions; do not add separate eligibility operations. Skip ineligible work without invoking reasoning or persisting a stall, while leaving authorization enforcement to the host.
    - [x] Have context loaders skip inactive intents and wrong-intent, settled, blocked, not-our-turn, or actionless negotiations before supplying briefing context; propagate rejected host reads unchanged rather than fabricating empty context or stalls. Scheduled skips create neither a hold nor pending principal work.
  - [x] Preserve host read rejections instead of substituting empty context or stalls; report background failures through `onError` and reject `reconcile()` on reconciliation read failures. Treat genuinely empty conversations and lists as valid data.
    - [x] Report background wake rejections through the configured `onError`, including cancellation as in v2; release the active slot on success or failure. Honor an explicitly pending follow-up after a failure, but do not automatically retry when no request is pending.
    - [x] Report background negotiation rejections through the same handler and release in-flight bookkeeping without retries or fabricated stalls; cleanup still allows an earlier genuine stall from another completed negotiation to reach the principal.
    - [x] Reject reconciliation's caller on its own intent-list, profile, or negotiation-list read failure, without substituting empty results or also reporting that failure through `onError`. Preserve v2's two-phase effects: completed membership updates and already scheduled wakes are not rolled back if recovery reads later fail.
  - [x] Submit returned turns through `submitTurn(opportunityId, { action, message, expectedTurnCount })`, taking the expected count from the negotiation record used for reasoning and leaving authorization, protocol validation, and stale-submission checks to the host. Await submission and propagate rejection without retrying or fabricating a stall.
  - [x] Implement `reconcile(): Promise<void>` using `listIntents`, with active-intent selection in the runner: initially adopt existing active intents without blanket wakes; on subsequent calls refresh membership with v2's newly-active wake behavior. Recover eligible turn-zero negotiations awaiting the principal through normal scheduling. Resolve after reads and scheduling, not background reasoning completion, and reject on reconciliation failure.
    - [x] Retain only known active intent IDs and whether initial adoption occurred, including an empty initial list. Later refreshes remove missing/inactive IDs and schedule wakes only for newly active IDs; execution still reads fresh records rather than using list snapshots as context.
    - [x] After membership refresh, read the principal profile and open negotiations in parallel; schedule only known-active, turn-zero negotiations awaiting the principal. Reuse normal in-flight exclusion, holds, fresh detailed eligibility checks, briefing, and submission.
    - [x] Keep membership current through serialized per-intent wake reads, not passive negotiation reads. Revalidate intent/list reads if membership changed while they were in flight, and replace the ID set on every confirmed wake observation, including unchanged inactive membership, so stale snapshots cannot suppress a later resumption.
    - [x] Serialize explicitly overlapping reconciliation calls so each performs fresh reads in order. A rejection must not poison later calls; do not retry failed reads automatically or wait for background reasoning to finish.
  - [x] Reuse normal scheduling for work found by host-triggered reconciliation; do not automatically run every existing intent or add polling, a durable queue, or distributed locking.
  - [x] Implement nonblocking `stop(): void` with runner-scoped cancellation and no restart; prevent new work and pending follow-up wakes without promising that active I/O has finished or persisted effects are undone. Leave transport teardown to the host, and do not claim restart recovery or cross-process exclusivity from process-local memory.
    - [x] Check cancellation around wake and single-negotiation asynchronous phases, before wake publication and each scheduling handoff, and pass the runner's `abortSignal` to both agents; already-started writes may finish, but later phases do not run after cancellation is observed. Both schedulers reject new work after stop, and `handle` ignores subsequent notifications before reads, scheduling, or hold release.
    - [x] Clear pending wakes and pending principal-work markers in `stop()` and ignore subsequent scheduling requests before reading host state. Keep active bookkeeping until each run settles; do not register late negotiation results or start completion-triggered wakes after stop, and do not wait for or roll back active I/O.
    - [x] Reject reconciliation after stop without host reads; queued calls check cancellation before starting, and active refreshes check it after reads and before scheduling. Already completed adoption or scheduling is not undone, and active host I/O may finish.
- [x] Implement `PrincipalAgent` in `src/agents/principal.agent.ts`.
  - [x] Define and export intent-bound constructor options, fresh wake/brief contexts, conversation/opportunity records, wake actions/results, and the narrow `PrincipalOperations` contract; move shared context records out of the runner and let `AgentHost` extend principal operations without upward agent imports.
  - [x] Construct and export `PrincipalAgent`, binding identity and dependencies without caching domain snapshots or starting work.
  - [x] Implement `wake(context)` on one instance per intent, using fresh context supplied by the runner; support notes, questions, question retirement, discovery, opportunity opening, and negotiation instructions through supplied operations.
  - [x] Keep counterparty retrieval and opportunity creation in one principal tool using `findCounterparties` and `createOpportunities`, preserving v2's merge, per-person deduplication, ranking, and batch limit; report created opportunity IDs for negotiation scheduling.
  - [x] Implement `brief(context)` with v2's one-step budget and single `set_brief` tool, confirmed-profile filtering, and bookkeeping-free H2A transcript; skip complete instructions and preserve no-output briefing as an empty action list without fabricated instructions or persistence.
  - [x] Preserve an empty action list as valid silence for an intent wake.
  - [x] Produce a concise brief and a standing decision (`continue`, `accept`, `decline`, or `stop`), keeping principal-facing actions distinct from A2A turns.
  - [x] Await incremental `onBrief` publication and retain its first failure for rejection after execution, without exposing it as model-correctable tool feedback.
  - [x] Keep intra-run opportunity updates on local copies so a rejected publication does not mutate caller-owned context or make later briefing skip an unpersisted instruction.
  - [x] Return actions/instructions without importing or calling the runner or negotiator.
- [x] Implement `NegotiatorAgent` in `src/agents/negotiator.agent.ts`.
  - [x] Construct and export the agent with bound principal/intent identity and execution dependencies, without host reads or startup work; define fresh negotiation context and result contracts beside it.
  - [x] Work one negotiation using its instruction and negotiation context, returning a protocol turn or a missing-fact/authority stall.
  - [x] Preserve the three-step budget, `submit_turn`/`stall` tools, one-result guard, and no-result stall; propagate execution failures and cancellation instead of converting them to stalls.
  - [x] Preserve the `propose`, `counter`, `accept`, and `decline` turn vocabulary while restricting `accept` to the original responder.
  - [x] Require and export `NegotiationRole` in the negotiation context; keep role-specific instructions, the model-visible action list, the tool schema, and turn-handler checks aligned without separate agent implementations.
  - [x] Exclude raw H2A transcripts, sibling opportunities, discovery, direct scheduling, and imports of the runner or principal agent.
  - [x] Move `Opportunity`, `Decision`, and `Stall` into the existing shared domain file now that both layers consume them; retain root exports without compatibility re-exports from the principal layer.
- [x] Implement genuinely shared reasoning mechanics in `src/agents/shared/`.
  - [x] Declare and export `Tool`, `ExecutionInput`, and `Execute` as package-owned contracts, with operation-specific opportunity IDs, required `abortSignal`, and documented whole-run budgets, ordering, output collection, and failure semantics.
  - [x] Implement and export `defineTool()` for typed tool authoring, use `Tool[]` at the execution boundary, and update principal briefing without changing its handler checks or adding schema validation.
  - [x] Implement the `Execute` function contract, `(input: ExecutionInput) => Promise<void>`, for direct model/tool loops with `createExecute(model)` in `reasoning.loop.ts`; keep domain result collection in agent-owned tool handlers. Hermes integration must supply the same whole-run contract externally without emulating individual model completions; keep its transport and native-session implementation outside the package.
  - [x] Pass the agreed identity and operation metadata through the execution boundary so external executors can distinguish principal wakes, one-opportunity briefings, and negotiations without parsing prompts or mixing H2A briefing history into A2A sessions.
  - [x] Keep the OpenRouter client and reusable direct tool loop in `src/agents/shared/`, configured by the host and exposed through the whole-run execution contract, without provider-specific code in the runner or agent reasoning policy.
    - [x] Export `createExecute` and its colocated `Model`, `ModelMessage`, `ToolCall`, and `ToolDefinition` contracts through `src/index.ts`. Bind only model access; keep transcripts and tool maps local to each run, pass prepared instructions/prompts unchanged, and send only tool metadata to the model.
    - [x] Implement and export `OpenRouterClient` and `OpenRouterClientOptions` in `openrouter.client.ts`, accepting the host's explicit API key without environment reads. Retain v2's one-to-three ordered models, default model list, 120-second request timeout, tool-aware routing, and error handling without retries. Combine caller cancellation with a per-request deadline covering the response body, and compose the client through `createExecute(model)`.
  - [x] Enforce the supplied `maxSteps` in the direct loop, retaining the agent-owned 8/1/3 budgets and v2's early completion when the model requests no tools; require the same limits from external execution. A final-budget response still runs all its tool calls and preserves collected outputs.
  - [x] Await tool handlers sequentially in the order requested by each model response; require the same ordering from external execution without serializing independent reasoning runs.
  - [x] Propagate runner-owned cancellation through `abortSignal`; check before and after model/tool calls and prioritize observed cancellation over ordinary tool-error feedback. Reject without promising interruption of already-running I/O or rollback of persisted effects.
  - [x] Return unknown-tool, invalid-JSON, and ordinary tool-handler errors to the model as tool results within the existing step budget; reject on model-request failure or observed cancellation, without an executor-level retry loop. Preserve string/JSON/null result serialization and empty-argument parsing without generic schema validation.
  - [x] Keep preparation of `instructions` and `prompt`, plus domain tool handlers, owned by their respective agent layers; share the `Tool` contract (`name`, `description`, `parameters`, `run`) and only other mechanics/contracts both layers actually use.
  - [x] Add a shared instruction helper used by both agents to explain the human-principal relationship, include a confirmed name when available, and supply the current intent and today's UTC date; keep this preparation independent of the direct or native executor.
    - [x] Use shared instruction preparation for principal briefing, wakes, and negotiation, without substituting routing IDs for human names.
  - [x] Keep shared files independent of both agents and the runner; keep temporary reasoning context scoped to its bounded execution.
- [x] Expose and verify the package's public entry point.
  - [x] Export `AgentRunner`, the agent layers, and required host/execution contracts explicitly through `src/index.ts`, without old API aliases or deep-import requirements.
  - [x] Replace placeholder package scripts and configure the exports, dependencies, and compilation needed by the implemented package; retain a private ESM package without runtime dependencies.
  - [x] Use `.js` relative specifiers so emitted declarations resolve for NodeNext as well as bundler consumers; verify the built public entry with strict declaration checking.
  - [x] Run package-local build, type checks, scoped ESLint, and whitespace checks; exercise focused behavior with offline in-memory probes without adding test files or making live model calls.
- [x] Complete package-local documentation.
  - [x] Update `docs/rewrite-plan.md` with finalized contracts, the implemented source tree, and a clear distinction between completed package work and Seref's external migration.
  - [x] Document host obligations for durable records, authoritative authorization, reconciliation, and cross-process delivery, distinguishing them from in-memory scheduling and event acceptance.
  - [x] Add `docs/standalone.md` using public exports to supply host operations and reasoning execution, forward events, and stop/unsubscribe without another scheduler or a drain guarantee.
  - [x] Typecheck the guide's snippets against built exports with NodeNext and bundler resolution; verify its launcher offline for subscription/reconciliation ordering, event delivery, reconnect failures, startup cleanup, cancellation, and non-draining shutdown.

## Live-model findings — 2026-09-21

Observed with OpenRouter in `agent-tui` using `05-users-05-intents-cofounders-and-project-collaborators.json` after synchronizing requirement enforcement and discovery progress from `agentv2`.

- [x] Prevent a principal's agent from inferring missing eligibility facts from the role they seek or from adjacent intent language. Bob's agent asked whether Alice had an engineering background and supported keyboard-first work. Alice's profile and intent established neither, but her negotiator claimed both and Bob accepted the connection on those invented facts. A requested collaborator role does not establish the principal's own role, and a general accessibility goal does not establish a keyboard-first preference. The principal layer must preserve such facts as unresolved in the brief so the negotiator stalls and the H2A agent asks the principal.
  - [x] Tighten principal briefing and tool guidance, and extract negotiation instructions into `src/agents/negotiator.instructions.ts` with explicit handling of missing and partially answered personal facts.
  - [x] Verify unknown, omitted-fact, partial-answer, confirmed-fact, and mismatch cases with a live model; replay the five-user scenario and the Alice/Bob clarification and resumption flow. Ask directly for the missing personal fact rather than substituting general interest in a match.
  - [x] Verify in `agent-tui` through `tmux`: an engineering-only answer keeps Alice's keyboard preference unresolved and sends no negotiation reply; explicitly confirming the preference resumes negotiation. Recheck every counterpart requirement after an answer, including facts omitted from an earlier brief.
- [ ] Use singular grammar in deterministic discovery progress: `Discovered 1 person`, not `Discovered 1 people`.
- [ ] Keep principal-facing discovery language consistent in generated notes. Despite the discovery instructions, one H2A note said `I am starting our search`. Strengthen the `note_principal` guidance to require `discover`/`reach out` and reject `search`/`searching` for user-facing activity descriptions.

## Jev scenario sweep — 2026-09-21

Tested all six bundled scenarios through `agent-tui` in `tmux`: 45 principals, 58 intents, and 25 human inputs produced 83 stalls, 68 questions, and 7 agreements, including follow-ups. Partial-answer holds, later approval, answer resumption, and explicit constraint enforcement generally worked; question quality remained mixed. All 337 Jev calls completed, averaging 450 ms. See the [local test report and transcripts](/private/tmp/index-agent-all-rhctO5/report.md).

- [ ] Preserve confirmed general facts across opportunities while keeping permission and interest specific to each opportunity. Mira confirmed mapping and visualization skills in her answer about Rosa, but the opportunity filter removed that human evidence from Jev's Salma evaluation. The brief retained the claim, so Jev stalled on an already confirmed skill. Reuse the skill evidence without treating interest in Rosa as permission to advance with Salma.
- [ ] Reuse or retire questions already covered by source evidence. Salma's overlapping pedestrian-heat question stayed pending after her shade/heat-route answer. Nadia was asked whether writing and sound should develop together even though her intent already said so. Check the intent and existing human answers before asking, and retire pending questions when an answer covers them.
- [ ] Ground decline reasons and generated claims in human evidence. Farah's agent asserted that she had no budget for paid design without a budget answer. Jev marked facts missing but also marked a contradiction, allowing the decline path. A role mismatch may justify declining without establishing a budget fact. Keep missing facts distinct from explicit contradictions and prevent unsupported claims from entering the reply; the same guard correctly asked about Dev's unknown budget later in the sweep.
- [ ] Keep clarification questions relevant to the stated intent and necessary for deciding whether to connect. The hiring principal was asked about talk rehearsals, cooling volunteers about energy-analysis or repair-organizing roles, and Sofia about cuisine preferences before an introduction. Review candidate relevance and counterpart follow-up questions so loosely related matches do not repeatedly ask principals to broaden their goals or settle unnecessary details.
- [ ] Investigate the principal-agent timeout in the community/climate fixture. Hana's paid-design wake timed out after roughly 120 seconds; one manual `Wake` recovered it in 18.3 seconds. Preserve the failure as a principal-execution error when reviewing reliability and recovery; the Jev decision calls completed successfully.
- [ ] Compare the previous and current implementations using matched scenarios and identical human answers before claiming a quality improvement. This sweep covered the current implementation once per fixture and stopped at idle checkpoints with some questions unanswered. Assess useful clarification, redundant questions, unsupported assertions, and recovery separately from raw stall counts.
