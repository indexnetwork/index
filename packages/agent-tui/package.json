{
  "name": "@indexnetwork/agent-tui",
  "version": "0.4.1",
  "private": true,
  "type": "module",
  "description": "A terminal scenario host for AgentRunner, H2A conversations, and A2A negotiations.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "start": "bun src/main.ts",
    "typecheck": "tsc --noEmit",
    "check": "bun run typecheck && eslint src",
    "build": "rm -rf dist && bun build ./src/index.ts --outdir dist --target bun --format esm --packages external --external '@indexnetwork/*' && tsc --project tsconfig.build.json"
  },
  "dependencies": {
    "@indexnetwork/agent": "workspace:*",
    "@opentui/core": "0.5.10",
    "@indexnetwork/protocol": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "1.3.14",
    "typescript": "5.9.3"
  }
}
