/**
 * Narrow summary port for opportunity discovery — IND-550.
 * Sources now route through the canonical negotiation module public surface.
 */
export { buildFallbackDigest } from "../negotiation/public/index.js";
